# 검증

## 실행

```powershell
.\scripts\verify.ps1
```

필요한 의존성을 이미 받은 환경에서는 `-Offline`을 붙일 수 있습니다.
Android SDK와 프로젝트의 Gradle JDK 설정이 필요합니다.

Windows의 한글 경로에서 Java 테스트 실행기가 테스트 클래스를 찾지 못하는 문제가 있어,
스크립트는 현재 소스를 임시 영문 경로로 복사한 뒤 다음 검사를 실행합니다.

- `testDebugUnitTest`: Robolectric 기반 데이터 이전·완료 상태·알람·화면 회귀 테스트와 날짜 계산 테스트
- `assembleDebug`: 디버그 APK 생성
- `lintDebug`: Android 정적 검사

성공하면 APK와 검사 보고서가 `app/build/verified`에 복사됩니다.
임시 검증 경로는 실행 결과에 표시됩니다. 실제 기기에 앱을 설치하거나 사용자 데이터를 변경하지 않습니다.

## 검증하는 동작

- 기존 JSON 및 순서 기반 완료 기록 이전, 기존 초기화 시간 이전
- 항목 삭제·중복 이름·새 항목 추가 후 완료 상태 유지
- 위젯과 개별 할 일 완료 동기화, 카테고리 간 독립성
- 앱 삭제 후 알람 취소, 구버전 알람 취소, 지연 수신 시 재등록 방지
- 이벤트 자동 초기화 방지
- 데일리·위클리·이벤트 화면 제목과 아이콘, 탭 이동·화면 재생성 후 임시 시간 복원
- 위젯 이벤트 탭의 선택 유지와 완료 처리, 오버레이 카테고리 동시 표시 및 빈 목록의 최소화 버튼
- 일일·주간 경계 시각, 복수 요일, 월 변경 및 서머타임

실제 게임 위에 표시되는 접근성 오버레이, 제조사별 절전 상태의 알람 동작은 별도 기기 검증이 필요합니다.
